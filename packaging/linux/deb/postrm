#!/bin/sh
set -eu

systemctl daemon-reload >/dev/null 2>&1 || true
# Deliberately preserve /var/lib/palladin-runtime and /etc/palladin. Package
# removal is not authorization to delete Agent identities or organization keys.
