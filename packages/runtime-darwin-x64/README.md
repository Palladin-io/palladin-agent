# @palladin/runtime-darwin-x64

Signed and notarized universal Palladin runtime bundle, packaged exclusively for macOS x64 npm installations. This package is installed automatically by `@palladin/agent` and contains no lifecycle scripts.
