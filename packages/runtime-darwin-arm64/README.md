# @palladin/runtime-darwin-arm64

Signed and notarized universal Palladin runtime bundle, packaged exclusively for macOS arm64 npm installations. This package is installed automatically by `@palladin/agent` and contains no lifecycle scripts.
