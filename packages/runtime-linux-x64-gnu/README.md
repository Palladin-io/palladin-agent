# `@palladin/runtime-linux-x64-gnu`

Native glibc x64 runtime for `@palladin/agent`. The client selects the Linux Convenience worker for ordinary interactive UIDs and the separately installed system broker for an explicitly authorized dedicated Agent UID. It has no npm lifecycle scripts and never installs privileged components.
