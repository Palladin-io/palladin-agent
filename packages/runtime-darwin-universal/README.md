# Palladin runtime for macOS

This platform package is installed by `@palladin/agent`. Its release artifact contains the signed and notarized universal `PalladinRuntime.app` bundle. It has no lifecycle scripts and is not a standalone JavaScript API.
