# `@palladin/runtime-linux-arm64-musl`

Native musl arm64 Convenience runtime for `@palladin/agent`, including Alpine Linux. It has no npm lifecycle scripts, privileged installer, glibc dependency, or plaintext fallback. Alpine Hardened is not supported in the MVP; this package never claims or installs that tier.
