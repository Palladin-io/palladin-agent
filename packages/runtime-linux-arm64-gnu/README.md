# `@palladin/runtime-linux-arm64-gnu`

Native glibc arm64 runtime for `@palladin/agent`. The client selects the Linux Convenience worker for ordinary interactive UIDs and the separately installed system broker for an explicitly authorized dedicated Agent UID. It has no npm lifecycle scripts and never installs privileged components.
